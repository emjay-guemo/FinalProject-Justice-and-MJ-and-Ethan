import java.util.ArrayList;

public class Deck {

    private ArrayList<Card> cards;

    /*
     * Deck Class
     * Responsibilities:
     * - Store all Card objects in a collection (ArrayList<Card>)
     * - Create a standard 52-card deck using all suits and ranks
     * - Shuffle the deck before a game starts
     * - Deal cards to players and hands
     * - Track how many cards remain in the deck
     * - Reset/rebuild the deck when starting a new game
     * Planned Methods:
     * - Deck()               // Constructor
     * - buildDeck()          // Creates all 52 cards
     * - shuffle()            // Randomizes card order
     * - dealCard()           // Returns and removes the top card
     * - cardsRemaining()     // Returns number of cards left
     * - isEmpty()            // Checks if deck is empty
     * - reset()              // Rebuilds and shuffles deck
     * Note:
     * Waiting for Card class implementation before creating
     * the deck generation logic.
     */

}
